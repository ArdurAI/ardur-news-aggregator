# Dependencies
node_modules/
.pnp/
.pnp.js

# Build output
dist/
build/
*.tsbuildinfo
.cache/

# Test / coverage
coverage/
.nyc_output/
test-results/
playwright-report/

# Runtime artifacts (generated snapshots are written here; never commit them)
data/runtime/
*.local.json

# ETL store — private article bodies (NEVER commit; bodies are for in-process
# extraction only and must not appear in any public mirror).
data/etl-store/

# Env / secrets
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.idea/
.vscode/
*.log
npm-debug.log*
